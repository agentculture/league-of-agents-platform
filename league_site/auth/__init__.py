"""Authentication for league-of-agents-platform.

Human OAuth lives in oauth.py / sessions.py (task t5); agent token
auth lives in tokens.py (task t6). Import modules directly - this
package init stays empty so parallel tasks never edit the same file.
"""
